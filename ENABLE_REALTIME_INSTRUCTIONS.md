# วิธีเปิด Realtime สำหรับตาราง orders ใน Supabase

## ขั้นตอนที่ 1: เข้า Supabase Dashboard
1. เปิด https://supabase.com/dashboard
2. เลือก Project ของคุณ
3. ไปที่เมนู **Database** (ทางซ้าย)

## ขั้นตอนที่ 2: เปิด Replication สำหรับตาราง orders
1. คลิกที่แท็บ **Replication** (อยู่ใต้ Database)
2. หาตาราง **`orders`** ในรายการ
3. เปิด toggle switch ให้เป็น **สีเขียว** (ON)
4. บันทึก

## ขั้นตอนที่ 3: ตรวจสอบ API Settings (ถ้ายังไม่ทำงาน)
1. ไปที่เมนู **Settings** → **API**
2. ในส่วน **Realtime**, ตรวจสอบว่าเปิดอยู่
3. ดูที่ **Realtime API URL** - ต้องมี URL แสดงอยู่

## ขั้นตอนที่ 4: Test ใน SQL Editor (Optional)
รัน SQL นี้เพื่อตรวจสอบว่า Replication เปิดแล้ว:

```sql
SELECT schemaname, tablename, 
       CASE WHEN relreplident = 'd' THEN 'default'
            WHEN relreplident = 'f' THEN 'full'
            WHEN relreplident = 'i' THEN 'index'
            WHEN relreplident = 'n' THEN 'nothing'
       END as replica_identity
FROM pg_class c
JOIN pg_namespace n ON c.relnamespace = n.oid
WHERE n.nspname = 'public' AND c.relname = 'orders';
```

ผลลัพธ์ที่ต้องการ: `replica_identity` ต้อง**ไม่เป็น** `nothing`

---

## หลังจากเปิด Realtime แล้ว
1. รีสตาร์ทแอป Flutter (ปิดแล้วเปิดใหม่)
2. ทดสอบอีกครั้ง
3. Order ใหม่ควรปรากฏทันทีโดยไม่ต้อง refresh! 🎉
